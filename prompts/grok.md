## Grok Specific Guidance

Grok (this harness) uses the skill at ~/.grok/skills/hashline/.

- The model is instructed to prefer hashline over raw search_replace.
- You will see the `hashline read` and `apply` calls in the scrollback (this is expected).

Base instructions:
{{base}}